; [pytest]
; testpaths = test_automation
; markers =
;     smoke: smoke tests
; [pytest]
; markers =
;     payment_flow: payment related test cases
;     p0: highest priority tests
;     staging_only: run only on staging
;     prod_blocker: must pass before production
; [pytest]
; markers =
;     payment_flow: tests for the payment flow



[pytest]
markers =
    smoke
    regression
    xray(key): Jira Xray Test Key
